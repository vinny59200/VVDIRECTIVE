import { ColorizeDirectiveDirective } from './colorize-directive.directive';

describe('ColorizeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorizeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
